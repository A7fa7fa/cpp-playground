

// static outside of a struct or class linits
// the variable or function to the translation unit and
// cannot be used outside of it
static int var = 5;
static void function() {};


// a static variabel/finction of a class are shared with all instances of that class
